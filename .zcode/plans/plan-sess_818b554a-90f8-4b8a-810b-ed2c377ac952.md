## План: Merge master → rust/pilot

### Шаг 1: Merge master в rust/pilot
```bash
git merge master
```
Это применит удаление C++ (1378 файлов, -501k строк) из master, сохраняя всю Rust работу с pilot. Ожидаемо конфликтов на C++ файлах — все резолвим в пользу master (удалить C++).

### Шаг 2: Резолв конфликтов
- C++ файлы в руте (edition/, meshers/, tests/ и т.д.) → принимаем удаление из master
- Файлы в rust/ → принимаем из rust/pilot (наша актуальная работа)
- register_types.cpp/.h → оставляем (нужны для Godot module compatibility)
- rust/cpp-baseline/ → сохраняем (parity harness)

### Шаг 3: Обновить документацию
- STATUS.md — обновить статус (теперь rust/pilot = чистый Rust + cpp-baseline reference)
- README.md — обновить инструкции сборки

### Шаг 4: Проверка
```bash
cargo test -p voxel-core -p voxel-gdext
cargo build -p voxel-gdext --release
```

### Шаг 5: Commit + push
